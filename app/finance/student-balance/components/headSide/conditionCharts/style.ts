import styled from "@emotion/styled";

export const FirstWrapper = styled.div`
  display: flex;
  gap: 10px;
  position: relative;

  .year-select {
    position: absolute;
    top: 20px;
    left: 14px;
  }
`;
