import styled from "@emotion/styled";
import { bgColors } from "styles/theme";

export const IELTSWrapper = styled.div`
  padding: 20px;
  border-bottom: 1px solid ${bgColors.whiteSmoke};

  .text {
    margin-bottom: 24px;
  }
`;
