import styled from "@emotion/styled";

export const WrapperDifferenceNumber = styled.div`
  background: #ffffff3d;
  border-radius: 4px;
  padding: 0 4px;
  display: flex;
  align-items: center;
  gap: 3px;
`;

export const Wrapper = styled.div`
  overflow: hidden;

  .foot-card {
    margin-top: 25px;
  }

  .scale {
    transform: scale(1);
  }
`;
