export enum EnumsStockStatus {
  ACTIVE = "100",
  INACTIVE = "200",
  // ARCHIVE = "300",
}

export enum StockProductType {
  Company = "100",
  Student = "200",
}

export enum StockSellPlace {
  Cashbox = "100",
  StudentPlatform = "200",
}

export enum StockStationary {
  Book = "100",
  Notebook = "200",
}

export enum StockActionType {
  Arrival = "100",
  Departure = "200",
  Transfer = "300",
  Change = "400",
  // Given = "600",
  // Sold = "700",
}
