export enum OFFICE_HOUR_DATE {
  MAIN_EXAM_DATE = 100,
  SPEAKING_EXAM_DATE = 200,
  REGULAR_DAY = 1000,
}

export enum OFFICE_HOURS {
  WAITING = 100,
  CONFIRMED = 200,
  CANCELLED = 300,
  RATING = 400,
  RATED = 500,
  CANDIDATE = 600,
}
